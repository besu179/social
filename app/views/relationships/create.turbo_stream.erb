<%= turbo_stream.replace "follow_form_#{@user.id}" do %>
  <%= render "users/follow_form", user: @user %>
<% end %>

<%= turbo_stream.prepend "followers",
  partial: "users/follower",
  locals: { user: @user } %>
